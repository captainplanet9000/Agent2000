{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt",
    "startCommand": "python run_ui.py"
  },
  "deploy": {
    "startCommand": "python run_ui.py"
  }
}
